{sub_dirs, ["src/apps/*"]}.

{erl_opts, [
	debug_info,
	fail_on_warning
]}.

{clean_files, ["*.eunit", "ebin/*.beam",
	"doc/*.html", "doc/*.edoc-info", "doc/*.css", "doc/*.png"]}.

{deps,
	[
	]
}.

{deps_dir, "deps"}.
